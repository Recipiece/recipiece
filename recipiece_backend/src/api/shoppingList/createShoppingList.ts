import { StatusCodes } from "http-status-codes";
import { AuthenticatedRequest } from "../../types";

export const createShoppingList = (request: AuthenticatedRequest) => {
  return [StatusCodes.NOT_IMPLEMENTED, {}]
}