describe("Issue Forgot Password Token", () => {
  it("should create a token and send an email", async () => {
    
  });

  it("should not allow multiple tokens to be issued in a short period of time for the same account", async () => {

  });

  it("should do nothing if the user does not exist", async () => {

  });
});
