export class TypeaheadInput {
  public static BUTTON_POPOVER_OPTION = (base?: string) => {
    return `button-popover-option-${base}`;
  }

  public static POPOVER_CONTAINER = (base?: any) => {
    return `popover-container-${base}`;
  }
}