export class NotFound {
  public static readonly BUTTON_GO_BACK = (base?: string): string => {
    return `button-go-back-${base}`;
  }

  public static readonly PARAGRAPH_MESSAGE = (base?: string): string => {
    return `paragraph-message-${base}`;
  }
}