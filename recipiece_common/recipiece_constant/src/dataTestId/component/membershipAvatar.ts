export class MembershipAvatar {
  public static readonly TOOLTIP_TRIGGER = (base?: any) => {
    return `membership-avatar-tooltip-trigger-${base}`;
  }

  public static readonly DISPLAY_CONTENT = (base?: any) => {
    return `membership-avatar-display-content-${base}`;
  }

  public static readonly TOOLTIP_CONTENT = (base?: any) => {
    return `membership-avatar-tooltip-content-${base}`;
  }
}