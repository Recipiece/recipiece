export class RecipeContextMenu {
  public static readonly DROPDOWN_MENU = (base?: string): string => {
    return `recipe-context-menu-${base}`;
  }

  public static readonly BUTTON_HALF_RECIPE = (base?: string): string => {
    return `button-half-recipe-${base}`;
  }

  public static readonly BUTTON_DOUBLE_RECIPE = (base?: string): string => {
    return `button-double-recipe-${base}`;
  }

  public static readonly BUTTON_SCALE_CUSTOM = (base?: string): string => {
    return `button-scale-custom-${base}`;
  }

  public static readonly BUTTON_RESET_CHANGES = (base?: string): string => {
    return `button-reset-changes-${base}`;
  }

  public static readonly BUTTON_ADD_TO_COOKBOOK = (base?: string): string => {
    return `button-add-to-cookbook-${base}`;
  }

  public static readonly BUTTON_AVAILABLE_COOKBOOK = (base?: string): string => {
    return `button-available-cookbook-${base}`;
  }

  public static readonly BUTTON_ADD_TO_SHOPPING_LIST = (base?: string): string => {
    return `button-add-to-shopping-list-${base}`;
  }

  public static readonly BUTTON_AVAILABLE_SHOPPING_LIST = (base?: string): string => {
    return `button-available-shopping-list-${base}`;
  }

  public static readonly BUTTON_FORK_RECIPE = (base?: string): string => {
    return `button-fork-recipe-${base}`;
  }

  public static readonly BUTTON_EDIT_RECIPE = (base?: string): string => {
    return `button-edit-recipe-${base}`;
  }

  public static readonly BUTTON_SHARE_RECIPE = (base?: string): string => {
    return `button-share-recipe-${base}`;
  }

  public static readonly BUTTON_REMOVE_FROM_COOKBOOK = (base?: string): string => {
    return `button-remove-from-cookbook-${base}`;
  }

  public static readonly BUTTON_DELETE_RECIPE = (base?: string): string => {
    return `button-delete-recipe-${base}`;
  }
}